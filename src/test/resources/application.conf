discovery {
  consul {
    host: "192.168.99.102"
    host: ${?CONSUL_HOST}
    host: ${?HOST}
    port: 8500
    port: ${?CONSUL_PORT}
  }
  advertised {
    port: 8080
    port: ${?ADVERTISED_PORT}
  }
}

test {
  greeting: "Hello Developer!!!!"
}
